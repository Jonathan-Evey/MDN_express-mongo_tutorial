extends layout

block content
    h1 #{title}: #{bookinstance.book.title}
    p #[strong Book Copy: #{bookinstance.imprint}]
    p Do you really want to delete this copy book #{bookinstance.book.title}?

    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(type='hidden', name='bookinstanceid', required='true', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete